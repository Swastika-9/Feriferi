<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Feriferi">

        <!-- 1. SUPER ADMIN DASHBOARD (The Shell Activity) -->
        <!-- We set this as the LAUNCHER so you can test your Admin UI immediately -->
        <activity
            android:name=".view.AdminDashboardActivity"
            android:exported="true"
            android:label="Super Admin Panel"
            android:theme="@style/Theme.Feriferi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Feriferi" />

        <activity
            android:name=".view.AddProductActivity"
            android:exported="true"
            android:label="Add Product"
            android:theme="@style/Theme.Feriferi"
            android:windowSoftInputMode="adjustResize" />

        <!-- 4. SELLER DASHBOARD -->
        <activity
            android:name=".view.SellerDashboard"
            android:exported="true"
            android:label="Seller Dashboard"
            android:theme="@style/Theme.Feriferi" />

    </application>

</manifest>
